import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ee.getid.ionic',
  appName: 'getid-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
